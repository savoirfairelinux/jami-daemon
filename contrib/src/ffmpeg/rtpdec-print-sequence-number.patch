From fa9d5308df42d2dcd596d57e3048515d9a18dbbe Mon Sep 17 00:00:00 2001
From: philippegorley <gorley.philippe@gmail.com>
Date: Thu, 15 Aug 2019 11:00:04 -0400
Subject: [PATCH] rtpdec: print sequence number

---
 libavformat/rtpdec.c | 1 +
 1 file changed, 1 insertion(+)

diff --git a/libavformat/rtpdec.c b/libavformat/rtpdec.c
index e75a34cb93..9b606c4e02 100644
--- a/libavformat/rtpdec.c
+++ b/libavformat/rtpdec.c
@@ -825,6 +825,7 @@ static int rtp_parse_one_packet(RTPDemuxContext *s, AVPacket *pkt,
         rtcp_update_jitter(&s->statistics, timestamp, arrival_ts);
     }

+    av_log(s, AV_LOG_WARNING, "rtp_dec seq=%d\n", s->seq);
     if ((s->seq == 0 && !s->queue) || s->queue_size <= 1) {
         /* First packet, or no reordering */
         return rtp_parse_packet_internal(s, pkt, buf, len);
--
2.17.1
