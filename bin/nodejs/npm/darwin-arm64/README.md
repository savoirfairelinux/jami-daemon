# @savoirfairelinux/jami-core-darwin-arm64

Platform-specific binary for `@savoirfairelinux/jami-core` — macOS arm64.

This package is installed automatically as an optional dependency of `@savoirfairelinux/jami-core`. You should not need to install it directly.
